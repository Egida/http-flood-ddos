groups:
- name: ddos_alert_group
  rules:

  # Traffic spike
  - alert: PossibleDDoS
    expr: rate(nginx_http_requests_total[10s]) > 200
    for: 5s
    labels:
      severity: critical
    annotations:
      description: "🚨 Traffic Nginx tăng mạnh → nghi ngờ HTTP Flood"

  # CPU server cao
  - alert: HighCPUServer
    expr: (1 - avg by (instance)(rate(node_cpu_seconds_total{mode="idle"}[15s]))) > 0.40
    for: 5s
    labels:
      severity: warning
    annotations:
      description: "🔥 CPU server vượt 40% → khả năng bị tấn công"

  # Xác nhận DDOS
  - alert: ConfirmedDDoS
    expr: |
      (rate(nginx_http_requests_total[10s]) > 200)
      and
      ((1 - avg by (instance)(rate(node_cpu_seconds_total{mode="idle"}[15s]))) > 0.40)
    for: 5s
    labels:
      severity: critical
    annotations:
      description: "🔥 XÁC NHẬN DDOS: traffic spike + CPU cao"
